name: shivams workflow

on:
  push:

jobs: 
  build:   # job name
    runs-on: ubuntu-latest   # GitHub Runner (Free)

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4   # clone repo

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Verify Docker
        run: docker --version

        